<% button = if @current_volunteer
     link_to 'Mám zájem', confirm_interest_path(request_id: request.id), method: :post, id: 'cta-zajem-o-poptavku-prihlaseny',
                                                                         class: "main-button secondary small"
   else
     link_to 'Mám zájem', login_path(redirect_to: confirm_interest_path(request_id: request.id)), id: 'cta-zajem-o-poptavku-neprihlaseny',
                               class: "main-button secondary small"
   end %>

<%= render partial: 'requests/request', locals: { request: request, button: button} %>