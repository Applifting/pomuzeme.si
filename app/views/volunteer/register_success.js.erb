var registrationModal = $('#registration');
var registrationErrors = $('#registration-errors-list');
var confirmationModal = $('#confirmation');

// clear old errors
registrationErrors.empty();

// hide registration modal
registrationModal.toggleClass('is-active');
$('html').toggleClass('is-clipped');

// toggle confirmation modal
confirmationModal.toggleClass('is-active');
$('html').toggleClass('is-clipped');